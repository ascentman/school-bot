"""Ізоляція тестів від локального середовища.

Виконується до імпорту school_bot, тому встигає вимкнути читання .env: інакше
тести звіряються з конфігом конкретної машини, а не з дефолтами коду.
"""

import os

os.environ["SCHOOL_BOT_NO_DOTENV"] = "1"
os.environ.pop("BOT_TOKEN", None)
